"""CloudBalancer stress-test CLI.

Submits realistic workloads across all implemented executor types
(SHELL, PYTHON, DOCKER, SIMULATED) against a running CloudBalancer
deployment, renders live progress, and produces a final summary report.
"""

__version__ = "0.1.0"
